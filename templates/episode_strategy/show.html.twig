{% extends 'base.html.twig' %}

{% block title %}EpisodeStrategy{% endblock %}

{% block body %}
    <h1>EpisodeStrategy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ episode_strategy.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ episode_strategy.title }}</td>
            </tr>
            <tr>
                <th>Length</th>
                <td>{{ episode_strategy.length }}</td>
            </tr>
            <tr>
                <th>Expression</th>
                <td>{{ episode_strategy.expression ? episode_strategy.expression|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_episode_strategy_index') }}">back to list</a>

    <a href="{{ path('app_episode_strategy_edit', {'id': episode_strategy.id}) }}">edit</a>

    {{ include('episode_strategy/_delete_form.html.twig') }}
{% endblock %}
